{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022"],
    // TypeScript 6.0 made `strict` default to true. This relay is intentionally
    // non-strict (loose Express handlers with untyped req/res), so keep it off
    // explicitly to preserve the pre-6.0 behavior.
    "strict": false,
    "esModuleInterop": true,
    "allowJs": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "moduleDetection": "force",
    "types": [
      "node",
      "mocha",
    ],
  },
  "include": [
    "src",
    "helpers",
    "config",
    "tests",
  ],
  "exclude": [
    "node_modules",
    "ssp-relay-enterprise",
    "package.json",
    "logs"
  ]
}